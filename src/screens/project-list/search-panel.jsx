
export const SearchPanel = () => {
	// useEffect(() => {
	// 	fetch('').then(response => {

	// 	})
	// }, [input])
	// const [state, setState] = useState(0)
	// const [list, setList] = useState(0)
	return (
		<form action="">
			<input />
			{/* <select>
				<option>负责人</option>
				{list.map(item => {
					return (
						<option>{item}</option>
					)
				})}
			</select> */}
		</form>
	)
}